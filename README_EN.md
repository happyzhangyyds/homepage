# MatrixCore Navigation

A minimal Vue navigation page with website links and a live weather clock.

```bash
npm install
npm run dev
npm run build
```

Edit `.env` for site and weather settings, and `src/assets/siteLinks.json` for links.
